# CloudBase AI 开发规则

本规则适用于所有基于腾讯云 CloudBase 的项目，AI 在生成、修改代码和配置时**必须严格遵守**。

## 1. 技术栈与架构
- **后端**：优先使用 CloudBase 云函数（Node.js 18+）、云数据库（NoSQL + MySQL）、云托管、云存储和静态托管。
- **前端/小程序**：可使用任意框架（如 React/Vue/Next.js），但后端接口必须调用 CloudBase。
- **禁止**：自行部署 Node.js/Java/Go 等传统服务器。

## 2. 身份认证与用户体系
- **微信小程序**：必须使用 `wx.cloud.getWXContext()` 获取 `OPENID`/`UNIONID`，禁止通过前端传参 `userId`。
- **Web 应用**：优先使用 CloudBase 内置的邮箱/手机号登录或第三方 OAuth，禁止在接口中直接信任前端传来的 `uid`。
- **安全**：所有受保护接口，必须通过 CloudBase 安全规则或云函数进行权限校验。

## 3. 数据库设计与安全
- **集合命名**：使用复数名词，如 `users`, `posts`，并添加注释说明字段含义。
- **索引**：为高频查询字段（如 `openid`, `status`, `createTime`）创建索引。
- **安全规则**：优先使用数据库安全规则进行行级权限控制，禁止编写"接口裸奔"的云函数。
- **禁止**：在前端直接暴露数据库 `doc(id)` 的写操作。

## 4. 云函数与后端逻辑
- **入口**：云函数统一使用 `exports.main = async (event, context) => {}` 格式。
- **日志**：关键步骤必须使用 `console.log/info/warn/error` 打印日志，便于排查问题。
- **错误处理**：必须捕获并处理异常，禁止静默失败。
- **禁止**：在云函数中硬编码 `secretId/secretKey` 等敏感信息，应使用环境变量或密钥管理。

## 5. 前端与 CloudBase SDK
- **初始化**：前端项目必须显式初始化 CloudBase SDK，并复用 `app` 实例。
- **调用**：所有后端请求必须通过云函数进行，禁止在前端直接调用数据库。
- **环境**：开发、测试、生产环境使用不同的 CloudBase 环境 ID，并通过配置文件管理。

## 6. 部署与运维
- **部署**：使用 CloudBase MCP 或 AI Toolkit 一键部署，禁止手动操作控制台。
- **CI/CD**：若使用 CI/CD，必须在流水线中配置 CloudBase 部署步骤。
- **回滚**：出现线上问题时，优先通过查看日志和调用链进行回滚或热修复。

## 7. AI 行为约束
- **读优先**：在修改代码前，必须先读取相关文件，理解现有实现。
- **解释变更**：每次修改代码时，必须说明变更原因，并评估对现有功能的影响。
- **最小改动**：优先采用重构和增量修改，避免不必要的重写。
- **安全审查**：涉及用户数据和支付逻辑时，必须进行安全审查，禁止生成存在风险的代码。
